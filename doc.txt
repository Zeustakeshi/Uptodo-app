
- change store and storage
+ update userInfo : 
    dispatch action setUserInfo(userInfo)
+ update tasks:
    dispatch action setTasks(tasks)

+ reset userIfo :
    dispatch action resetUserInfo
+ reset tasks
    dispatch action resetTasks

// lưu dữ liệu habit vào storage va firebase
// cho phép người dùng xóa habit
// thêm chức năng search habit

